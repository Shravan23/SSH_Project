{
  "name": "ssh_project",
  "version": "1.0.0",
  "description": "Web-based Docker container management with terminal interface",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "prestart": "docker pull ubuntu:latest",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "docker",
    "terminal",
    "ssh",
    "container-management"
  ],
  "author": "Shravan Bachu",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "dockerode": "^4.0.0",
    "xterm": "^5.3.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
